<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent">



    <TextView
    android:id="@+id/text_view_title"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:text="@string/get_to_know_old_trees"
    android:gravity="center"
    android:textStyle="bold"
    android:layout_marginTop="20dp"
    style="@style/TextAppearance.AppCompat.Large"
    app:layout_constraintTop_toTopOf="parent"/>

    <ImageView
        android:id="@+id/image_view_first"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:src="@drawable/first_tree"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toBottomOf="@id/text_view_title"
        android:layout_marginTop="30dp"/>

    <TextView
        android:id="@+id/text_view_first"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/description_home_screen"
        android:textColor="#000"
        android:gravity="center"
        android:layout_margin="16dp"
        app:layout_constraintTop_toBottomOf="@id/image_view_first"/>

    <ImageView
        android:id="@+id/image_view_second"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:layout_margin="20dp"
        android:scaleType="centerCrop"
        android:src="@drawable/second_tree"
        app:layout_constraintTop_toBottomOf="@id/text_view_first"/>

        <TextView
            android:id="@+id/text_view_second"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/description_home_screen_second"
            android:textColor="#000"
            android:gravity="center"
            android:layout_margin="16dp"
            app:layout_constraintTop_toBottomOf="@id/image_view_second"/>


</androidx.constraintlayout.widget.ConstraintLayout>
    </ScrollView>
